CREATE TABLE NFCE (
	ID INTEGER NOT NULL,
	ID_CLIENTE INTEGER,
	ID_USUARIO INTEGER,
	ID_CAIXA INTEGER,
	ID_VENDEDOR INTEGER,
	ID_MOVIMENTO INTEGER NOT NULL,
	CFOP INTEGER NOT NULL,
	NFCE_NUMERO INTEGER,
	NFCE_NATUREZA_OPERACAO VARCHAR(65),
	NFCE_MODELO CHAR(2),
	NFCE_SERIE VARCHAR(20),
	NFCE_DATA_EMISSAO DATE,
	NFCE_DATA_SAIDA DATE,
	NFCE_CHAVE_ACESSO VARCHAR(44),
	NFCE_CHAVE_ACESSO_ORIGINAL VARCHAR(44),
	NFCE_PROTOCOLO VARCHAR(100),
	NFCE_AMBIENTE INTEGER,
	NFCE_ENVIO VARCHAR(20),
	NFCE_DH_EMISSAO TIMESTAMP,
	NFCE_DH_AUTORIZACAO VARCHAR(30),
	NFCE_STATUS VARCHAR(30),
	NFCE_NRO_RECIBO VARCHAR(50),
	DATA_VENDA DATE,
	HORA_VENDA VARCHAR(8),
	VALOR_VENDA DECIMAL(18,6),
	TAXA_DESCONTO DECIMAL(18,6),
	DESCONTO DECIMAL(18,6),
	TAXA_ACRESCIMO DECIMAL(18,6),
	ACRESCIMO DECIMAL(18,6),
	VALOR_FINAL DECIMAL(18,6),
	VALOR_RECEBIDO DECIMAL(18,6),
	TROCO DECIMAL(18,6),
	VALOR_CANCELADO DECIMAL(18,6),
	TOTAL_PRODUTOS DECIMAL(18,6),
	TOTAL_DOCUMENTO DECIMAL(18,6),
	BASE_ICMS DECIMAL(18,6),
	ICMS DECIMAL(18,6),
	ICMS_OUTRAS DECIMAL(18,6),
	ISSQN DECIMAL(18,6),
	PIS DECIMAL(18,6) DEFAULT 0,
	COFINS DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO_ITENS DECIMAL(18,6),
	DESCONTO_ITENS DECIMAL(18,6),
	STATUS_VENDA CHAR(1),
	NOME_CLIENTE VARCHAR(100),
	CPF_CNPJ_CLIENTE VARCHAR(14),
	CUPOM_CANCELADO CHAR(1),
	IND_DESCONTO_GERAL CHAR(1),
	IND_ACRESCIMO_GERAL CHAR(1),
	NOME_COMPUTADOR VARCHAR(100),
	NOME_CAIXA VARCHAR(30),
	LEMBRETE VARCHAR(800),
	DATA_CANCELAMENTO DATE,
	NFE CHAR(1),
	FCP_VALOR NUMERIC(18,2) DEFAULT 0,
	FCP_VALORST NUMERIC(18,2) DEFAULT 0,
	FCP_VALORRET NUMERIC(18,2) DEFAULT 0,
	TIPO_IMPORT VARCHAR(20),
	SAT_NRO_SESSAO INTEGER,
	RECUPERA_VENDA VARCHAR(50),
	NFCE_NUMERO_AUX INTEGER,
	HASH_ID VARCHAR(600),
	TIPO_OPERACAO VARCHAR(20),
	ID_TROCA INTEGER,
	ID_CLIENTE_CREDITO INTEGER,
	VALOR_TROCA NUMERIC(18,2),
	VALOR_CREDITO NUMERIC(18,2),
	NFCE_CNF INTEGER,
	TOTAL_CUSTOS NUMERIC(18,2) DEFAULT 0,
	IMPORTACAO_ID INTEGER,
	IMPORTACAO_ORIGEM VARCHAR(30),
	NFCE_FURO VARCHAR(1),
	CONSTRAINT INTEG_141 PRIMARY KEY (ID)
);
CREATE INDEX DAV_IDX10 ON NFCE (NFCE_MODELO);
CREATE INDEX DAV_IDX11 ON NFCE (DATA_VENDA);
CREATE INDEX DAV_IDX9 ON NFCE (IMPORTACAO_ORIGEM);
CREATE INDEX I_NFCE_NUMERO ON NFCE (NFCE_NUMERO);
CREATE INDEX NFCE_IDX1 ON NFCE (NFCE_NUMERO);
CREATE INDEX NFCE_IDX2 ON NFCE (NFCE_NUMERO DESC);
CREATE INDEX NFCE_IDX3 ON NFCE (ID_USUARIO);
CREATE INDEX NFCE_IDX4 ON NFCE (DATA_VENDA);
CREATE INDEX NFCE_IDX5 ON NFCE (STATUS_VENDA);
CREATE INDEX NFCE_IDX6 ON NFCE (NFCE_STATUS);
CREATE INDEX NFCE_IDX7 ON NFCE (NFCE_DATA_SAIDA);
CREATE INDEX NFCE_IDX8 ON NFCE (ID);
CREATE UNIQUE INDEX RDB$PRIMARY21 ON NFCE (ID);



CREATE TABLE NFCE_ITENS (
	ID INTEGER NOT NULL,
	ID_PRODUTO INTEGER NOT NULL,
	ID_NFCE INTEGER NOT NULL,
	CFOP INTEGER NOT NULL,
	GTIN VARCHAR(14),
	ITEM INTEGER,
	QUANTIDADE DECIMAL(18,6) DEFAULT 0,
	VALOR_UNITARIO DECIMAL(18,6) DEFAULT 0,
	VALOR_CUSTO NUMERIC(18,6) DEFAULT 0,
	VALOR_TOTAL DECIMAL(18,6) DEFAULT 0,
	TOTAL_ITEM DECIMAL(18,6) DEFAULT 0,
	BASE_ICMS DECIMAL(18,6) DEFAULT 0,
	TAXA_ICMS DECIMAL(18,6) DEFAULT 0,
	ICMS DECIMAL(18,6) DEFAULT 0,
	TAXA_DESCONTO DECIMAL(18,6) DEFAULT 0,
	DESCONTO DECIMAL(18,6) DEFAULT 0,
	TAXA_ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	TAXA_ISSQN DECIMAL(18,6) DEFAULT 0,
	ISSQN DECIMAL(18,6) DEFAULT 0,
	TAXA_PIS DECIMAL(18,6) DEFAULT 0,
	PIS DECIMAL(18,6) DEFAULT 0,
	TAXA_COFINS DECIMAL(18,6) DEFAULT 0,
	COFINS DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO_RATEIO DECIMAL(18,6) DEFAULT 0,
	DESCONTO_RATEIO DECIMAL(18,6) DEFAULT 0,
	CST CHAR(3),
	CANCELADO CHAR(1),
	MOVIMENTA_ESTOQUE CHAR(1),
	ICMS_ST VARCHAR(4),
	ID_GRADE_ITENS INTEGER,
	FCP_BASE NUMERIC(18,2) DEFAULT 0,
	FCP_TAXA NUMERIC(18,2) DEFAULT 0,
	FCP_TAXA_ST NUMERIC(18,2),
	FCP_VALOR NUMERIC(18,2) DEFAULT 0,
	FCP_VALOR_ST NUMERIC(18,2),
	FCP_BASE_ST NUMERIC(18,2),
	FCP_VALOR_STRET NUMERIC(18,2),
	FCP_TAXA_STRET NUMERIC(18,2),
	FCP_BASE_STRET NUMERIC(18,2),
	ICMS_BC_STRT NUMERIC(18,2),
	ICMS_TAXA_STRT NUMERIC(18,2),
	ICMS_VALOR_STRT NUMERIC(18,2),
	ANP_GLP_TAXA NUMERIC(18,4),
	ANP_GNN_TAXA NUMERIC(18,4),
	ANP_GNI_TAXA NUMERIC(18,4),
	ANP_GLP_PESO_LIQ NUMERIC(18,4),
	CSOSN VARCHAR(3),
	ICMS_EFETIVO_BC NUMERIC(18,2) DEFAULT 0,
	ICMS_EFETIVO NUMERIC(18,2) DEFAULT 0,
	ICMS_EFETIVO_RED_BC NUMERIC(18,4) DEFAULT 0,
	ICMS_EFETIVO_TAXA NUMERIC(18,4) DEFAULT 0,
	ICMS_DESONER_MOT VARCHAR(3),
	ICMS_DESONER NUMERIC(18,2),
	ICMS_MONO_RET_VALOR NUMERIC(18,2),
	ICMS_MONO_RET_TAXA NUMERIC(18,6),
	ICMS_MONO_RET_QTDE NUMERIC(18,6),
	ANP_CODIGO VARCHAR(15),
	RED_BASE_ICMS NUMERIC(18,6),
	CONSTRAINT INTEG_146 PRIMARY KEY (ID)
);
CREATE INDEX I_NFCE_ITENS_ ON NFCE_ITENS (ID_NFCE);
CREATE INDEX NFCE_ITENS_IDX1 ON NFCE_ITENS (CANCELADO);
CREATE INDEX NFCE_ITENS_IDX2 ON NFCE_ITENS (ID_PRODUTO);
CREATE UNIQUE INDEX RDB$PRIMARY22 ON NFCE_ITENS (ID);


CREATE TABLE NFCE_TOTAL_TIPO_PGTO (
	ID INTEGER NOT NULL,
	ID_VENDA_CABECALHO INTEGER NOT NULL,
	ID_TIPO_PAGAMENTO INTEGER NOT NULL,
	NOME_COMPUTADOR VARCHAR(100),
	NOME_CAIXA VARCHAR(30),
	VALOR DECIMAL(18,6),
	NSU VARCHAR(30),
	ESTORNO CHAR(1),
	REDE VARCHAR(50),
	CARTAO_DC CHAR(1),
	BANDEIRA VARCHAR(100),
	TEF_1VIA BLOB SUB_TYPE TEXT,
	TEF_2VIA BLOB SUB_TYPE TEXT,
	CONSTRAINT INTEG_150 PRIMARY KEY (ID)
);
CREATE INDEX I_NFCE_TOTAL_TIPO_PGTO_ ON NFCE_TOTAL_TIPO_PGTO (ID_VENDA_CABECALHO);
CREATE INDEX NFCE_TOTAL_TIPO_PGTO_IDX2 ON NFCE_TOTAL_TIPO_PGTO (ID_TIPO_PAGAMENTO);
CREATE UNIQUE INDEX RDB$PRIMARY23 ON NFCE_TOTAL_TIPO_PGTO (ID);



DDL DAV:
CREATE TABLE DAV (
	ID INTEGER NOT NULL,
	ID_CLIENTE INTEGER,
	ID_ORCAMENTO INTEGER,
	ID_USUARIO INTEGER,
	ID_CAIXA INTEGER,
	ID_VENDEDOR INTEGER,
	ID_MOVIMENTO INTEGER NOT NULL,
	CFOP INTEGER NOT NULL,
	DATA_VENDA DATE,
	HORA_VENDA VARCHAR(8),
	VALOR_VENDA DECIMAL(18,6) DEFAULT 0,
	TAXA_DESCONTO DECIMAL(18,6) DEFAULT 0,
	DESCONTO DECIMAL(18,6) DEFAULT 0,
	TAXA_ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	VALOR_FINAL DECIMAL(18,6) DEFAULT 0,
	VALOR_RECEBIDO DECIMAL(18,6) DEFAULT 0,
	TROCO DECIMAL(18,6) DEFAULT 0,
	VALOR_CANCELADO DECIMAL(18,6) DEFAULT 0,
	TOTAL_PRODUTOS DECIMAL(18,6) DEFAULT 0,
	TOTAL_DOCUMENTO DECIMAL(18,6) DEFAULT 0,
	BASE_ICMS DECIMAL(18,6) DEFAULT 0,
	ICMS DECIMAL(18,6) DEFAULT 0,
	ICMS_OUTRAS DECIMAL(18,6) DEFAULT 0,
	ISSQN DECIMAL(18,6) DEFAULT 0,
	PIS DECIMAL(18,6) DEFAULT 0,
	COFINS DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO_ITENS DECIMAL(18,6) DEFAULT 0,
	DESCONTO_ITENS DECIMAL(18,6) DEFAULT 0,
	STATUS_VENDA CHAR(1),
	NOME_CLIENTE VARCHAR(100),
	CPF_CNPJ_CLIENTE VARCHAR(14),
	CUPOM_CANCELADO CHAR(1),
	IND_DESCONTO_GERAL CHAR(1),
	IND_ACRESCIMO_GERAL CHAR(1),
	NOME_COMPUTADOR VARCHAR(100),
	NOME_CAIXA VARCHAR(30),
	SITUACAO VARCHAR(30),
	LEMBRETE VARCHAR(800),
	ENDERECO_CLIENTE VARCHAR(300),
	FONE_CLIENTE VARCHAR(11),
	NFE CHAR(1),
	DATA_CANCELAMENTO DATE,
	DESTINO VARCHAR(30),
	NRO_DESTINO INTEGER,
	HASH_ID VARCHAR(600),
	ID_TROCA INTEGER,
	ID_CLIENTE_CREDITO INTEGER,
	VALOR_TROCA NUMERIC(18,2),
	VALOR_CREDITO NUMERIC(18,2),
	TOTAL_CUSTOS NUMERIC(18,2) DEFAULT 0,
	MODO VARCHAR(20),
	INDICADOR VARCHAR(20),
	IMPORTACAO_ID INTEGER,
	IMPORTACAO_ORIGEM VARCHAR(30),
	CONSTRAINT INTEG_193 PRIMARY KEY (ID)
);
CREATE INDEX DAV_IDX1 ON DAV (STATUS_VENDA);
CREATE INDEX DAV_IDX2 ON DAV (VALOR_VENDA);
CREATE INDEX DAV_IDX3 ON DAV (MODO);
CREATE INDEX DAV_IDX4 ON DAV (DATA_VENDA);
CREATE INDEX DAV_IDX5 ON DAV (STATUS_VENDA);
CREATE UNIQUE INDEX RDB$PRIMARY31 ON DAV (ID);





DDL DAV_FORMA_PAGAMENTO:
CREATE TABLE DAV_TOTAL_TIPO_PGTO (
	ID INTEGER NOT NULL,
	ID_VENDA_CABECALHO INTEGER NOT NULL,
	ID_TIPO_PAGAMENTO INTEGER NOT NULL,
	NOME_COMPUTADOR VARCHAR(100),
	NOME_CAIXA VARCHAR(30),
	VALOR DECIMAL(18,6),
	NSU VARCHAR(30),
	ESTORNO CHAR(1),
	REDE VARCHAR(10),
	CARTAO_DC CHAR(1),
	CONSTRAINT INTEG_198 PRIMARY KEY (ID)
);
CREATE INDEX DAV_TOTAL_TIPO_PGTO_IDX2 ON DAV_TOTAL_TIPO_PGTO (ID_TIPO_PAGAMENTO);
CREATE INDEX I_DAV_TOTAL_TIPO_PGTO_ ON DAV_TOTAL_TIPO_PGTO (ID_VENDA_CABECALHO);
CREATE UNIQUE INDEX RDB$PRIMARY36 ON DAV_TOTAL_TIPO_PGTO (ID);



DDL DAV_ITENS:
CREATE TABLE DAV_ITENS (
	ID INTEGER NOT NULL,
	ID_PRODUTO INTEGER NOT NULL,
	ID_DAV INTEGER NOT NULL,
	CFOP INTEGER NOT NULL,
	GTIN VARCHAR(14),
	ITEM INTEGER,
	QUANTIDADE DECIMAL(18,6) DEFAULT 0,
	VALOR_UNITARIO DECIMAL(18,6) DEFAULT 0,
	VALOR_CUSTO NUMERIC(18,6) DEFAULT 0,
	VALOR_TOTAL DECIMAL(18,6) DEFAULT 0,
	TOTAL_ITEM DECIMAL(18,6) DEFAULT 0,
	BASE_ICMS DECIMAL(18,6) DEFAULT 0,
	TAXA_ICMS DECIMAL(18,6) DEFAULT 0,
	ICMS DECIMAL(18,6) DEFAULT 0,
	TAXA_DESCONTO DECIMAL(18,6) DEFAULT 0,
	DESCONTO DECIMAL(18,6) DEFAULT 0,
	TAXA_ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO DECIMAL(18,6) DEFAULT 0,
	TAXA_ISSQN DECIMAL(18,6) DEFAULT 0,
	ISSQN DECIMAL(18,6) DEFAULT 0,
	TAXA_PIS DECIMAL(18,6) DEFAULT 0,
	PIS DECIMAL(18,6) DEFAULT 0,
	TAXA_COFINS DECIMAL(18,6) DEFAULT 0,
	COFINS DECIMAL(18,6) DEFAULT 0,
	ACRESCIMO_RATEIO DECIMAL(18,6) DEFAULT 0,
	DESCONTO_RATEIO DECIMAL(18,6) DEFAULT 0,
	CST CHAR(3),
	CANCELADO CHAR(1),
	MOVIMENTA_ESTOQUE CHAR(1),
	ICMS_ST VARCHAR(4),
	ID_GRADE_ITENS INTEGER,
	INDICADOR_COMPOSICAO_UNICO_ITEM VARCHAR(1),
	ID_PRODUTO_COMPOSICAO INTEGER,
	CONSTRAINT INTEG_195 PRIMARY KEY (ID)
);
CREATE INDEX DAV_ITENS_IDX1 ON DAV_ITENS (CANCELADO);
CREATE INDEX DAV_ITENS_IDX2 ON DAV_ITENS (ID_PRODUTO);
CREATE INDEX I_DAV_ITENS_ ON DAV_ITENS (ID_DAV);
CREATE UNIQUE INDEX RDB$PRIMARY33 ON DAV_ITENS (ID);